<% content_for(:title, (t '.title') ) %>
<div class="container">
  <div class="row">
    <div class="col-lg-8 offset-lg-2">
      <h1><%= t '.title' %></h1>
      <%= render partial: 'form', locals: { action: 'edit' } %>
    </div>
  </div>
</div>
