$('#error_explanation').empty()
<% if @comment.errors.any? %>
  $('#error_explanation').prepend("<%= j(render 'error',{ comment: @comment }) %>");
<% else %>
  $('#js-table-comment').prepend("<%= j(render 'comment',{ comment: @comment }) %>");
  $('#comment_body').val('')
<% end %>
